# ==================== Emby 求片系统配置文件 ====================
# 复制此文件为 .env 并填入你的实际配置
# 
# ⚠️ 必须修改的配置项已标记 [必须]
# 其他配置已迁移到管理后台，此处仅保留基础配置
#
# 📌 注意：LICENSE_KEY 授权码请在 docker-compose.yml 中配置
#
# 📌 管理后台：首次启动时会自动生成默认账号密码和入口路径
#    请查看 Docker 日志获取默认配置，登录后必须修改

# ==================== Flask 配置 [必须] ====================
# SECRET_KEY: 用于会话加密，必须设置为随机字符串
# 生成命令: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-random-string

# 调试模式（生产环境必须设为 False）
FLASK_DEBUG=False
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# ==================== 数据库配置 [必须] ====================
# 如果 MySQL 在宿主机运行：
#   - Linux: 使用宿主机 IP（如 192.168.1.100）
#   - Mac/Windows Docker Desktop: 使用 host.docker.internal
# 如果 MySQL 在 Docker 网络中：使用容器名

DB_HOST=host.docker.internal
DB_PORT=3306
DB_USER=emby_user
DB_PASSWORD=your_mysql_password
DB_NAME=emby_request
